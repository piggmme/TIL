// .replace(/[.]+/g, '.')
// .replace(/(^[.])|([.]$)/g, '')
// .replace(/^$/g, 'a')
// .replace(/.+/g, match => (match.length >= 16 ? match.substring(0, 15) : match))
// .replace(/[.]$/, '')
// .replace(/.+/