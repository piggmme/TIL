    // if (board[1][0] === 0) {
    //   cost = Array.from({ length: n }, () => Array(n).fill(Number.MAX_SAFE_INTEGER));
    //   queue.push([1, 0, 1]);
    //   cost[1][0] = 100;
    //   answer = Math.min(answer, BFS());
    // }