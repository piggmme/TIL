{
  "scripts": {
    "start": "run-p watch serve",
    "sass": "sass sass:css",
    "watch": "npm run sass -- --watch",
    "serve": "live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.38.1"
  }
}
